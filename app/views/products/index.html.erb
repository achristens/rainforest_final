<section>
  <div class="all-intro">
    <h1>Skincare</h1>
    <p>We believe in the Korean skin-first philosophy—instead of covering up skin issues with makeup, it’s best to treat the root cause before they start. With the right knowledge and vetted selection of products, you'll have only good (skin) days ahead!</p>
  </div>
</section>

<section class="all-main">
  <div class="search-form">
    <p>Looking for something specific? Search below or browse our full selection.</p>
    <%= form_tag(products_path, :method => "get", id: "search-form") do %>
      <%= text_field_tag :search, params[:search], placeholder: "Search products" %>
      <%= submit_tag "Search" %>
      <% if @products.present? %>
        <% @products.each do |product| %>
        <% end %>
      <% else %>
        <p>No luck!</p>
      <% end %>
    <% end %>
  </div>
  <div class="all-products">
      <% @products.each do |product| %>
        <div class="product-each">
          <p><%= link_to(image_tag(product.url), "/products/#{product.id}") %></p>
          <p class="product_name"><%= link_to product.name, product_path(product) %></p>
          <p><%= product.price_in_dollars %></p>
        </div>
      <% end %>
  </div>
</section>

<!-- "/products/#{product.id}" -->
